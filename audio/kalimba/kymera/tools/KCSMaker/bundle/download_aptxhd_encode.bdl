import imp
import os
cap_list = imp.load_source("mod", os.path.abspath(os.path.join(os.path.curdir,"bundle/config/capabilities.cfg")))

# List of capabilities to be bundled up in KDCs
KDCs = {}
KDCs["download_aptxhd_encode"] = [dnld_aptxhd_encode]
